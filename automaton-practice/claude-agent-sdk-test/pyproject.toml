[project]
name = "claude-agent-sdk-test"
version = "0.1.0"
description = "Claude Agent SDK Bonus Project"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "claude-agent-sdk>=0.2.138",
    "ipykernel>=7.3.0",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
    "jupyter>=1.1.1",
]

[tool.uv.workspace]
members = [
    "ui-demo",
]
